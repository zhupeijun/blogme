<h1>Listing article_tags</h1>

<table>
  <thead>
    <tr>
      <th>Article</th>
      <th>Tag</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @article_tags.each do |article_tag| %>
      <tr>
        <td><%= article_tag.article_id %></td>
        <td><%= article_tag.tag %></td>
        <td><%= link_to 'Show', article_tag %></td>
        <td><%= link_to 'Edit', edit_article_tag_path(article_tag) %></td>
        <td><%= link_to 'Destroy', article_tag, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Article tag', new_article_tag_path %>
